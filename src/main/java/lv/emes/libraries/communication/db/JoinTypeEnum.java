package lv.emes.libraries.communication.db;

/**
 * Enum of SQL join types.
 * Supported ones are LEFT OUTER JOIN, LEFT OUTER JOIN and INNER JOIN.
 *
 * @author eMeS
 * @version 2.0.
 */
public enum JoinTypeEnum {

    LEFT, RIGHT, INNER
}
